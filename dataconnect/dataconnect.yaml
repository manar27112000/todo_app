specVersion: "v1beta"
serviceId: "todo_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "todo_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
